import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.appTarea.app',
  appName: 'appTarea',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
